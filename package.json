{
  "name": "example-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "front": "yarn workspace front",
    "admin": "yarn workspace admin",
    "shared": "yarn workspace shared",
    "reinstall": "rm -rf ./node_modules && rm -rf ./packages/*/node_modules && yarn install"
  },
  "devDependencies": {
    "eslint-import-resolver-alias": "^1.1.2",
    "prettier": "^2.2.1",
    "webpack-filter-warnings-plugin": "^1.2.1"
  }
}
